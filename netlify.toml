[build]
  command = "npm run build"
  publish = "dist"

# Vite 8 は Node ^20.19.0 || >=22.12.0、Astro 5 は Node >=18.20.8 を要求する。
# 指定しないとNetlifyのデフォルトNode（サイト作成時期依存）が使われ、
# ローカルでは通るビルドが本番だけ失敗するため明示的に固定する。
[build.environment]
  NODE_VERSION = "22"
