# CLAUDE - 東京路線図アプリ プロジェクト概要

## プロジェクト概要
このプロジェクトは東京の電車路線図を表示し、駅間の経路検索機能を提供するWebアプリケーションです。

### 📋 サービス概要
本サイトは利用者が出発駅と到着駅に関連する路線のみを絞って表示できる「**インタラクティブ路線図サービス**」です。推薦システムでは補えない運行イレギュラー時（遅延・運休など）にも、自分で判断できる簡略化された路線図を提供することを目的としています。

### 技術スタック
- **フロントエンド**: Astro + React + TypeScript
- **地図表示**: Leaflet + React-Leaflet
- **開発サーバー**: `npm run dev` (localhost:8080)
- **ビルド**: `npm run build`

### プロジェクト構成
```
src/
├── components/           # Reactコンポーネント
│   ├── RailwayMap.tsx   # メイン地図コンポーネント
│   ├── StationSelector.tsx    # 駅選択UI
│   ├── RouteRecommendations.tsx # 経路推薦表示
│   ├── SimpleMap.tsx    # シンプル地図表示
│   └── DataTest.tsx     # データテスト用
├── data/                # 路線データ（39路線）
│   ├── routes.ts        # 全路線統合定義
│   ├── yamanote.ts      # 山手線データ
│   ├── chuo.ts          # 中央線データ
│   ├── ginza-line.ts    # 銀座線データ
│   └── ...              # その他路線データ
├── utils/
│   └── routeFinder.ts   # 経路検索アルゴリズム
└── pages/
    └── index.astro      # メインページ
```

### 主要機能
1. **インタラクティブ路線図表示**
   - 39路線の東京電車路線（JR、地下鉄、私鉄、新交通システム）
   - ズームレベル対応駅表示
   - 路線の表示/非表示切り替え
   - 駅クリック時の通過路線情報表示
   - お台場エリア（りんかい線・ゆりかもめ）および首都圏広域（つくばエクスプレス）をカバー
   - 神奈川県方面（南武線・根岸線）の重要路線を追加

2. **経路検索機能**
   - 出発駅・到着駅選択
   - 最適経路推薦（最短時間、乗換回数考慮）
   - 複数経路候補表示

3. **UI機能**
   - 乗換駅のみ表示フィルター
   - 折りたたみ可能な操作パネル
   - レスポンシブデザイン（限定的）
   - **路線表示切替機能**: デフォルト非表示、プログラムで制御可能
     - 状態変数: `showRouteToggleSection` (boolean)
     - 個別路線の表示/非表示切り替え
     - 経路推薦数の設定（1-10件）
     - 全路線表示/非表示の一括切り替え
     - 時間フィルター設定

4. **所要時間フィルター機能（現在実装中）**
   - 出発駅から指定時間内にアクセス可能な駅のみを表示
   - 5分〜60分の範囲で設定可能
   - 乗換を含む最短経路時間で計算
   - デバッグ用の一致性チェック機能付き

5. **駅クリック時刻表ツールチップ**
   - 地図上の駅アイコンをクリックするとツールチップを表示
   - **左カラム**: その駅を通る全路線一覧（経路上の路線は太字＋路線色のボーダーで強調、「乗」ラベル付き）
   - **右カラム**: 選択路線の次発5本（出発時刻・種別・行先）
   - **ルート方向の厳守**: 表示する時刻は選択中の経路の「進行方向」に合致したものに限る
     - `stationTimelineMap` に各駅の経路セグメント情報（`routeKey`, `directionIndex`, `depTime`）を保持
     - 経路上の路線が時刻データなしの場合（例: 小田急江ノ島線）は「乗車路線ですが時刻データなし」と表示し、無関係な路線にフォールバックしない
     - ユーザーが左カラムで非経路路線を選択した場合は「ルート外参考」ラベルを付与
   - **出発時刻設定**: ツールチップ内および駅選択パネルで変更可能（`timetableBaseTime` を共有）
   - ヘッダーに「出発に設定」「到着に設定」ボタンを配置

### 時刻表データ実装方針

**重要**: 時刻表ツールチップの右カラムには「選択中の経路で実際に乗車する路線の、進行方向に合った発車情報」のみを表示する。

- 各路線の時刻データは `src/data/timetableData.ts` の `timetableLines` 配列に登録する
- 各路線の `directions` は `[下り/片方向, 上り/もう一方向]` の2要素配列にすること
- `directionIndex` は `getDirectionIndex(routeKey, fromStation, toStation)` で自動判定される
- 時刻データがない路線（例: 小田急江ノ島線）はツールチップ左列に表示されるが右列はデータなしと表示
- 新路線の時刻データを追加する際は `timetableLines` に push するだけで自動的にツールチップに反映される

**現在時刻データ対応路線** (src/data/timetableData.ts の `timetableLines`):
- 山手線 (`yamanote`)
- 東海道線 (`jrTokaidoMainLine`)
- 中央・総武線 (`chuo`)
- 東京メトロ銀座線 (`ginzaLine`)
- 東京メトロ丸ノ内線 (`marunouchiLine`)
- 東急東横線 (`tokyuToyokoLine`)
- 東急田園都市線 (`tokyuDenEnToshiLine`)
- 小田急小田原線 (`odakyuLine`)
- 京急線 (`keikyuLine`)
- 京浜東北線 (`keihinTohoku`)

### データ構造
- 各駅は緯度経度座標を持つ
- 路線は駅の配列として定義
- 駅間の移動時間は距離ベースで算出

### 路線カバレッジ分析（39路線）

#### JR線（13路線）
- **環状・幹線**: 山手線（31駅）、中央線、京浜東北線、東海道線、総武線
- **郊外・広域**: 常磐線、埼京線、高崎線、東海道本線、武蔵野線、横浜線
- **神奈川方面**: 南武線（立川〜川崎）、根岸線（大船〜横浜）

#### 東京メトロ（9路線）
- 銀座線、丸ノ内線、日比谷線、東西線、千代田線
- 有楽町線、半蔵門線、南北線、副都心線

#### 都営地下鉄（4路線）
- 浅草線、三田線、新宿線、大江戸線

#### 私鉄（10路線）
- 小田急線、小田急江ノ島線、京王線
- 東急東横線、東急田園都市線
- 西武池袋線、西武新宿線、東武東上線
- 京急線、京成本線

#### 市営地下鉄・新交通システム（3路線）
- **横浜市営地下鉄ブルーライン**: 横浜エリア
- **東京モノレール**: 浜松町〜羽田空港
- **りんかい線**: 大崎〜新木場（お台場エリア）

#### 新交通システム・広域鉄道（2路線）
- **ゆりかもめ**: 新橋〜豊洲（お台場観光・ビジネスエリア）
- **つくばエクスプレス**: 秋葉原〜つくば（首都圏広域高速鉄道）

**カバレッジ特徴:**
- **都心部**: 山手線・地下鉄網による完全カバー
- **観光地**: お台場エリア（りんかい線・ゆりかもめ）を完全接続
- **広域エリア**: 茨城県つくば（つくばエクスプレス）まで拡大
- **神奈川方面**: 南武線・根岸線で川崎・横浜方面を強化
- **乗換最適化**: 主要ターミナル駅での効率的な接続
- **総駅数**: 約400駅以上をカバー

## コミットに関する重要な規則

**2026-07-30更新: ユーザー承認により、事前確認不要の運用に変更。ただし品質チェック（テスト・ビルド）を必ず通すことで安全性を担保する。**

### 1. チェック通過を条件に自律的にコミットしてよい
- コミット前に毎回ユーザーに確認を取る必要はない
- ただし、コミットする前に必ず以下を実行し、**全て通過することを確認すること**
  - `npm run test:types`（型チェック）
  - `npm run test:unit`（ユニットテスト）
  - `npm run build`（ビルド）
  - UI/フロントエンドに影響する変更は、可能であれば `npm run test:e2e` も実行する
- チェックが失敗した場合はコミットせず、原因を修正するか、ユーザーに報告すること
- 破壊的操作（force push、履歴の書き換え、他ブランチへのpushなど）は引き続き事前確認が必要

### 2. 論理的な単位でのコミット
- **まとめてコミットしない**
- 一つの論理的な変更単位ごとにコミットすること
- 関連のない変更は別々のコミットに分けること
- 各コミットは単独で理解可能で、意味のある変更を含むこと

### 3. コミットメッセージ
- 変更内容を簡潔に説明すること
- 何を変更したかではなく、なぜ変更したかを重視すること

### 4. ブランチ運用（GitHub Flow）

**2026-08-05更新: ユーザー承認により、GitHub Flowでの自律運用に変更。**

- `main` は常にデプロイ可能な状態を保つ。直接コミットしない
- 新しいタスクに着手する際は、`main` から作業ブランチを自律的に作成してよい
  （例: `git fetch origin main && git checkout -B <branch-name> origin/main`）
- 作業・コミット・pushはこれまでどおり作業ブランチ上で行う
- ブランチの作業が完了したら、`main` へのPRを自律的に作成する
- CIがグリーンであることを確認してから、自律的にPRをマージしてよい
- 既存のPRにひもづく作業ブランチが既にマージ済みの場合、そのブランチに追加でコミットせず、
  `main` から新しいブランチを切り直すこと
- 破壊的操作（force push、履歴の書き換えなど）は引き続き事前確認が必要

## 例
良い例：
- チェック（型・テスト・ビルド）を通過してから行う機能追加のコミット
- チェックを通過してから行うバグ修正のコミット
- チェックを通過してから行うリファクタリングのコミット
- `main` から作業ブランチを切り、CIグリーンを確認してPRをマージする一連の流れ

悪い例：
- 複数の無関係な変更をまとめたコミット
- テスト・ビルドが失敗したままのコミット
- CIが赤いままのマージ、または無許可でのforce push

## 🚫 データに関する絶対ルール

### ヒートマップ・駅統計データは実データのみ使用すること

**AIが生成・推定した値をデータとして使用することを禁止する。**

- `src/data/station-stats-data.json` に保存するデータは、実測・公開データのみ
- 現在の実データソース: **Overpass API（OpenStreetMap）** で収集した以下の6項目のみ
  - `convenienceStoreCount`（コンビニ数）
  - `restaurantCount`（飲食店数）
  - `cafeCount`（カフェ数）
  - `supermarketCount`（スーパー数）
  - `hospitalCount`（病院・クリニック数）
  - `parkAreaM2`（公園面積）
- データがない駅は **灰色（`HEATMAP_NO_DATA_COLOR = '#aaaaaa'`）で表示** する。ゼロや推定値で埋めない
- 新しいフィールドを追加する場合は、**実際のデータソース（API・公開統計）を明記** すること
- 「それっぽい値」「概算」「AIによる推計」は一切使用禁止

**背景:** 2026年6月以前のデータには家賃・乗降客数・治安スコアなどAI生成の推定値が混入していたが、ユーザーが誤った情報に基づいて意思決定することを防ぐため、全て削除した。

---

## デプロイ

**2026-08-13更新: 本番反映はユーザーの目視確認を経てから行う。E2Eは省略可。**

本番へは「プレビュー更新 → ユーザーが実機で目視確認 → OKが出たら `main` にマージ」
の順で反映する。目視確認を飛ばして `main` にマージしないこと。

E2E（`npm run test:e2e`）は時間がかかるため省略してよい。
`test:types` / `test:unit` / `build` は従来どおり必須。

`npm run deploy:prod` を直接実行する場合は以下（サンドボックスからは
Netlify CLI が未認証のため実行できない）：

```bash
# テスト通過後に本番デプロイ（推奨）
npm run deploy:prod

# プレビューのみ（テストなし・本番URL変わらず）
npm run deploy:preview

# 直接デプロイ（テストをスキップ、緊急時のみ）
netlify deploy --prod
```

### テストが通らない場合

- WSL2環境ではブラウザ依存ライブラリ不足でPlaywrightが動かない場合がある
- その場合は `npm run test:e2e:prod` で本番URLに対してテストを実行するか、
  WindowsのPowerShellから `npx playwright test` を実行する

## 現在の重要な問題点と解決策

### ✅ 解決済み: 小田急線の経路推薦が表示されない (2026-07-31解決)

**問題の詳細:**
- 藤沢→新宿の経路検索で、小田急線経由のルートが推薦結果の上位に出てこなかった
- 理論的には「藤沢（小田急江ノ島線）→ 相模大野 → 新宿（小田急小田原線）」のルートが有力（乗り換え1回）

**根本原因:**
- 駅登録・乗り換え検出自体は正常に機能していた（相模大野駅は両路線に正しく登録済み）
- 真因は `odakyu-line.ts` / `odakyu-enoshima-line.ts` の `timeToNext` が**各駅停車ベースの生値**だったこと
- `routeFinder.ts` の所要時間計算は区間内の全駅の `timeToNext` を単純合計する方式のため、駅数の多い小田急線（新宿-小田原間46駅）は各駅停車換算で相模大野→新宿だけで54分と算出され、急行ベースで計算されているJR幹線に対して不当に不利な所要時間になっていた
- 結果、実際には10件中8位程度に埋もれ、デフォルトのUI表示件数内でも実質見えない状態になっていた

**修正内容:**
- `odakyu-line.ts` / `odakyu-enoshima-line.ts` の `timeToNext` を急行相当の平均所要時間（各駅停車の実測値の約0.56〜0.62倍）に補正
- `tests/unit/utils/routeFinder.test.ts` に「藤沢→新宿の小田急線経由ルートが上位3件以内に入る」回帰テストを追加

**ファイル:** `src/data/odakyu-line.ts`, `src/data/odakyu-enoshima-line.ts`, `tests/unit/utils/routeFinder.test.ts`

**既知の制約:** この修正は小田急線データのみの補正であり、駅数の多い他路線（京急線・京成線など）でも同様の各駅停車ベース／急行ベースの時間データ混在問題が起きる可能性がある。全路線データの時間モデルを統一する設計改善は未着手。

---

### 🔧 既知の問題: 経路推薦で重複ルートが表示される

**問題の詳細:**
- 横浜→新宿の経路検索で、実質的に同じ乗り換えパターンを持つルートが複数表示される
- 意味のないバリエーションが生成されてユーザビリティが低下

**解決状況:** 一部改善済み（重複除去アルゴリズム実装済み）、完全解決は小田急線問題解決後に対応予定

---

### 🔧 既知の問題: 小さすぎる文字サイズ（2026-08-15調査・未着手）

**調査方法:** モバイル幅(430px)で実際に描画されたテキストの computed font-size を全要素から収集。

**判定基準（12px を共通の下限とする）:**

| 基準 | 下限 |
|---|---|
| Apple HIG | 11pt（本文17pt） |
| Material Design | 12sp（caption が最小） |
| GOV.UK / BBC GEL 等 | 本文16px、補助14px、絶対下限12px |
| WCAG 2.2 | 絶対値の規定なし（1.4.4 で200%拡大可能なことのみ要求） |

WCAG違反ではないが、可読性のガイドラインは全て下回っている。

**検出結果: 12px未満が51種**

| サイズ | 件数 | 該当箇所 |
|---|---|---|
| 9px | 37 | 地図上の駅名ラベル、折りたたみの `▼` |
| 10px | 8 | 並び順ボタン（あいうえお/色/登録順/近い順）、全表示/全非表示、`▼ 他 480 路線を表示` |
| 11px | 6 | 「現在地から」、セクション見出し（駅ラベル/データ可視化/駅フィルター/表示切替）、`▶` |

**最も深刻なのは駅名ラベルの9px。** 件数が最多で、かつアプリの主コンテンツそのもの。
原因は倍率の既定値:

```ts
// src/components/RailwayMap.tsx
const [stationSizeScale, setStationSizeScale] = useState(0.8);
const stationLabelFontSize = Math.round(11 * stationSizeScale);  // → 9px
```

ふりがな表示時はさらに `lfs * 0.75` で約7pxまで落ちる
（`Math.max(7, ...)` で7pxが下限として明示されている）。
`FS.micro`(9px) / `FS.tiny`(10px) という定数自体も12pxを下回っている。

**修正案:**

1. **駅ラベル**: `stationSizeScale` の既定を 0.8→1.0、または基準を 11px→12px に上げる
   （1.0倍で12px確保）。ふりがなの下限7pxも10px程度に
2. **10pxのボタン類**: `FS.tiny`(10px) を12pxに引き上げ
3. **11pxの見出し**: `FS.helper`(11px) を12pxに引き上げ
4. `FS.micro` / `FS.tiny` は定数ごと見直し

**注意:** 駅ラベルを大きくすると地図上でラベル同士が重なりやすくなる。
表示密度とのトレードオフのため、まず倍率だけ上げて重なり具合を確認するのが安全。

---

### 🔧 既知の問題: データの欠損（2026-08-15調査・未着手）

総駅数 6,001 / 総路線数 490 に対する充足率。

| データ | 充足率 | 未対応 |
|---|---|---|
| 英語の駅名 | 100% | 0 |
| 中国語の駅名 | 100% | 1 |
| 路線名の翻訳 | 100% | 0 |
| **駅の読み（かな）** | **9.6%** | 5,425駅 |
| **韓国語の駅名** | **17.7%** | 4,936駅 |
| **時刻表** | **17.8%**（87/490路線） | 403路線 |
| **駅ナンバリング** | **22.7%** | 4,639駅 |
| **駅統計（ヒートマップ）** | **25.7%** | 4,460駅 |

**駅の読み（9.6%）が最優先。** 駅名検索の前方一致・かな検索に使われるため、
9割の駅は「ふじさわ」等のかな入力で検索できず、漢字か英語でしか引けない。

**駅統計は地域が完全に偏っている:**

| 地域 | カバレッジ |
|---|---|
| 関東 | 1,527/1,765（87%） |
| 中部・東海 | 0/1,641（0%） |
| 東北 | 0/605（0%） |
| 近畿 | 0/572（0%） |
| 中国・四国 | 0/557（0%） |
| 北海道 | 0/346（0%） |
| 九州ほか | 14/515（3%） |

`scripts/collect-heatmap-data.py` のコメントに「対象: 首都圏主要路線 330駅」と
明記されており、関東偏重は仕様どおり。対象範囲を広げれば埋められるが、
1駅1クエリ・成功時4秒待機のため残り4,460駅で5時間以上かかる。
なおサンドボックスから Overpass API への疎通は未確認。

**時刻表が無い主要路線（駅数上位）:** 名鉄名古屋本線58駅、JR日豊本線55駅、
JR東北本線49駅、JR常磐線（上野〜いわき）49駅、近鉄大阪線44駅、京阪本線41駅。
関西・中京の大手私鉄が丸ごと未対応。

**データ破損の疑い（未解決）:**
- `豊橋市 豊根口`（飯田線）: 実在駅を特定できず。三河川合の可能性が高いが
  出馬・上市場も欠落しており断定できない
- `ものおい`（真岡線）: 同線に実在しない駅名

## 開発者向け制御機能

### 路線表示切替セクションの表示制御

**概要:**
路線表示切替セクション（Route Display Toggle）は、デフォルトで非表示に設定されており、プログラムから動的に表示/非表示を制御できます。

**制御方法:**
```typescript
// RailwayMap.tsx 内で以下の状態変数を操作
const [showRouteToggleSection, setShowRouteToggleSection] = useState(false);

// 表示する場合
setShowRouteToggleSection(true);

// 非表示にする場合
setShowRouteToggleSection(false);
```

**含まれる機能:**
- 個別路線の表示/非表示切り替え
- 経路推薦数の設定（1, 2, 3, 5, 10件）
- 全路線表示/非表示の一括操作
- 時間フィルター設定（5分〜60分）
- デバッグ用テストボタン（山手線・中央線ポップアップ）

**実装場所:** `src/components/RailwayMap.tsx:64, 1222-1538`

**現在の状態:** デフォルトで非表示（showRouteToggleSection = false）

## モバイル・レスポンシブデザインの現状

### 📱 現在のモバイル対応レベル: **基本対応済み（改善余地あり）**

#### ✅ 実装済みの対応
1. **基本的なFlexbox対応**
   - `StationSelector.tsx:147`: `flexWrap: 'wrap'` で小画面対応
   - 駅選択フィールドは `minWidth: '200px', maxWidth: '300px'` で可変幅

2. **折りたたみUI**
   - 駅選択パネル、経路推薦、凡例が折りたたみ可能
   - 小画面では自動的に内容を隠してスペース節約

3. **可変幅コンポーネント**
   - 経路推薦: `minWidth: '200px'`
   - ボタン類: 適切な `padding` とホバー効果

#### ❌ 不足している対応
1. **メディアクエリなし**
   - CSS `@media` クエリが一切使用されていない
   - 画面サイズに応じた詳細な調整がない

2. **固定幅問題**
   - 一部コンポーネントで固定幅（`300px`等）を使用
   - 非常に小さな画面（320px以下）での表示問題の可能性

3. **タッチ操作最適化不足**
   - ボタンサイズがタッチ操作に最適化されていない
   - タップターゲットが44px未満の可能性

4. **地図コンポーネント**
   - Leaflet地図のモバイル最適化が不明
   - 駅マーカーのサイズ調整が画面サイズ非依存

#### 🔧 推奨する改善点
1. **メディアクエリ追加**
   ```css
   @media (max-width: 768px) {
     /* タブレット対応 */
   }
   @media (max-width: 480px) {
     /* スマートフォン対応 */
   }
   ```

2. **タッチ最適化**
   - ボタン最小サイズ: 44px × 44px
   - 入力フィールドの高さ調整

3. **レイアウト改善**
   - 縦積みレイアウトの強化
   - 余白とパディングの画面サイズ対応

4. **文字サイズ調整**
   - 小画面での読みやすさ向上
   - 重要情報の視認性確保

**調査ファイル:**
- `src/components/StationSelector.tsx:147-252` (flexWrap実装箇所)
- `src/components/RouteRecommendations.tsx:68,154,253` (minWidth設定)
- `src/components/RailwayMap.tsx` (地図表示ロジック)

## 📄 利用規約・法的情報

### 🗄️ データについて
- **データソース**: 駅や路線の位置情報は自作データ、またはオープンデータ（例: 国土交通省や各自治体が公開しているGTFSデータ）を使用
- **正確性**: データの正確性については保証できません。必ず公式情報と併せてご利用ください
- **ライセンス**: オープンデータを利用する場合は、出典・ライセンス表記を明記すること

### 🎨 表示・デザインについて
- 路線図のデザインは独自描画であり、鉄道会社公式のデザイン・商標・アイコンは利用していません
- 路線色や表示スタイルは便宜上のもので、公式の配色とは異なる場合があります

### ⚠️ 利用上の注意
- 本サービスは各鉄道事業者とは一切関係ありません
- 提供する情報は目安であり、運行状況や正確な所要時間は必ず公式の時刻表・運行情報をご確認ください

### 📝 著作権・利用規約
- **© 2025 Flex Rail Map Project**
- 無断転載禁止。研究・教育目的での利用は歓迎しますが、再配布の際は出典を明記してください
- 広告収益化を行う場合も、データのライセンス条件に従い出典をフッター等に表記してください
- **このサイトはClaude Codeを用いて作られました**

### 🔗 推奨フッター例
```
© 2025 Flex Rail Map Project
駅データは独自作成またはオープンデータを利用しています。
本サービスは非公式であり、各鉄道事業者とは関係ありません。
Made with Claude Code
```

## 🔧 開発ガイドライン

### デザインの一元管理（アトミックデザイン）

UI（色・フォントサイズ・余白・角丸・ボタン・タッチ領域）を書くときは、値を直接書かず
定義元から取ること。**同じ値・同じ規則を2箇所目に書こうとしたら、書く前に共通化する。**

- 判断表と現状の調査結果: **`docs/design-system.md`**
- 作業時に読むスキル: **`.claude/skills/design-tokens/SKILL.md`**
- 現状を数え直す: `node scripts/audit-design-tokens.mjs`

**UIコンポーネントはアトミックデザインの3層に分ける。
どの層に置くかは「何を知っているか」で決める（見た目の複雑さではない）。**

| 層 | 置き場所 | 知ってよいこと | 例 |
|---|---|---|---|
| atoms | `src/components/ui/atoms/` | デザイントークンだけ | `Button` `IconButton` `Chip` `TextField` `Select` `Switch` ほか |
| molecules | `src/components/ui/molecules/` | アトムの組み合わせ方 | `SegmentedControl` `Stepper` |
| organisms | `src/components/`, `src/components/legend/` | アプリの概念（路線・駅）とデータ | `RouteSwitchBoard` `StationSelector` |

判定に迷ったら「路線」「駅」という語をその部品から消せるかを見る。消せないなら organism。

**新しくボタン・入力欄・チップを書かないこと。** 既存のアトムを使う。
**アトム以外で `<button>` `<select>` `<textarea>` `<input>` を書くとテストが落ちる**
（`tests/unit/components/ui/noRawControls.test.ts`）。
どのアトムを使うかの一覧は `docs/design-system.md` の「どの部品を使うか」。

**寸法は `ui/atoms/controlSize.ts` の `CONTROL_SIZE` だけから取る。**
段階は `md`(44px, 指で押すもの) と `sm`(24px, 補助操作) の2つのみ。
**同じ行・同じグループに並ぶ操作は必ず同じ段階にする**
（隣り合う部品で高さが違うのが「揃っていない」の主な原因だった）。

**状態は塗りで示す。** 選択・非選択で枠線の太さや文字の太さを変えると、
押すたびに外形が動いて並びがずれる。

色は `SEMANTIC.*`（出発=緑 / 到着=赤 / primary=青）、白黒は `NEUTRAL.*`、
それ以外は `getThemeColors(theme)` から取る。直書きはテストで落ちる
（`tests/unit/constants/semanticColors.test.ts`）。

**文字サイズは `FS`、余白は `L.sp`、角の丸みは `L.r` から取る。**
`fontSize: '12px'` のような直書きはテストで落ちる
（`tests/unit/components/ui/noRawSizes.test.ts`）。

- 文字サイズは **12px が下限**。段階は caption(12) / body(14) / input・title(16) /
  heading(20) / display(24) の5つ。12px未満の段階を足すとテストが落ちる
  （例外はふりがなのみ。ルビなので本文の75%・下限10px）
- 角の丸みは値ではなく役割で選ぶ。control(8px, 操作部品) / card(12px, 箱) /
  pill(999px, バッジ)
- 段階に無い値は近い段階に丸める。新しい段階を足す前に既存で足りないか確認すること
寸法の一致は `tests/unit/components/ui/atoms.test.tsx` が固定している。

このプロジェクトは「同じ規則を2箇所に書いて片方だけ直す」不具合を繰り返しているため
（入力欄だけ色が変わる、駅アイコンの片方だけタッチ領域が広がる等）、
様式美ではなく再発防止として扱う。

### プレビューデプロイルール

**2026-08-13更新: プレビューでの目視確認を挟む運用に変更。E2Eは省略可。**

作業がひと区切りついたらプレビューを更新し、URLをユーザーに共有すること。

**運用フロー:**
1. 作業ブランチにコミット・push する（品質チェックは従来どおり必須）
2. プレビューが更新されたことをユーザーに伝える
3. **ユーザーが実機で目視確認する**
4. 目視でOKが出たら `main` にマージして本番反映する

- 手順3のユーザー確認を飛ばして `main` にマージしないこと
- E2E（`npm run test:e2e`）は時間がかかるため省略してよい。
  代わりに `test:types` / `test:unit` / `build` は必ず通すこと

#### 🔴 push したら必ずプレビューURLを書く（2026-09-02追加）

**push・デプロイのたびに、返答の中にプレビューURLをそのまま書くこと。**
ユーザーに聞かれてから答えるのでは遅い。目視確認はこの運用の必須手順であり、
URLが無ければユーザーは確認に着手できない。

- 「プレビューを更新しました」だけで URL を書かないのは**不可**
- 「前と同じURLです」で済ませるのも**不可**。毎回そのまま書く
- リンクは省略・短縮せず、クリックできる形で全文を書く
- あわせて push したコミットのハッシュも書く（どの版を見ているか分かるように）
- 複数ブランチに push したときは、実際に見てほしい方のURLを書く

貼るURL:

```
https://claude-project-loading-ktm7lo--flex-railway-map.netlify.app
https://preview--flex-railway-map.netlify.app
```

サンドボックスからは Netlify に到達できないためビルド完了は確認できない。
その旨も一言添える（「ビルド中の可能性があります」）。

**プレビューURLを固定にする:**

PRごとの `deploy-preview-<番号>--flex-railway-map.netlify.app` はPR番号が変わると
URLも変わってしまう。ブランチ名は固定なので、Netlifyの **Branch deploys** を
有効にすると次のURLが常に使える。

```
https://claude-project-loading-ktm7lo--flex-railway-map.netlify.app
```

Netlify管理画面の Site configuration → Build & deploy → Branch deploys で
対象ブランチ（`claude/project-loading-ktm7lo`）を許可すると有効になる。
（`/` と `_` はハイフンに置換され、小文字化されたものがサブドメインになる）

サンドボックスから `npm run deploy:preview` は実行できない（Netlify CLI が
未認証で `NETLIFY_AUTH_TOKEN` も無い）。push によるNetlifyの自動ビルドを使う。

- 本番デプロイ（`--prod`）はユーザーの明示的な指示があった場合のみ実行する

### CHANGE.log 記録ルール

**セッション中に行った変更は必ず `CHANGE.log` に記録すること。**

- セッション終了時（またはまとまった作業が一区切りついたとき）に、そのセッションで行った変更を `CHANGE.log` に追記する
- フォーマットは既存のエントリに倣う（日付・セッション見出し → `###` 単位の変更概要）
- feat/fix/improve/docs のプレフィックスを使い、対象ファイルも明記する
- ユーザーから「CHANGE.logに記録して」と言われなくても、自発的に記録する

### コミットルール

**重要な開発ルール（2026-08-05更新: GitHub Flowでの自律運用に変更、品質チェックとCIで担保）:**
1. **チェック通過を条件に自律的にコミット・push可**
   - `npm run test:types` / `npm run test:unit` / `npm run build` を実行し、全て通過してからコミットする
   - チェックが失敗する場合はコミットせず、修正するかユーザーに報告する
   - `main` から作業ブランチを作成・切り替えしてよい（GitHub Flow）。詳細は上記「ブランチ運用（GitHub Flow）」を参照
   - force pushや履歴の書き換えなど破壊的操作は引き続き事前確認が必要

2. **論理的な単位でコミットすること**
   - **まとめてコミットしない**
   - 一つの論理的な変更単位ごとにコミットする
   - 関連のない変更は別々のコミットに分ける
   - 各コミットは単独で理解可能で、意味のある変更を含む

3. **コミットメッセージの品質**
   - 変更内容を簡潔に説明する
   - 何を変更したかではなく、なぜ変更したかを重視する
   - 日本語で記述し、機能追加は `feat:`、改善は `improve:`、修正は `fix:` で始める

**良いコミットの例:**
- 機能追加のコミット: `feat: 所要時間表示を数字のみの円形デザインに統一`
- バグ修正のコミット: `fix: 小田急線経路推薦が表示されない問題を修正`
- 改善のコミット: `improve: 駅枠線の二重・三重表示に白い間隔を追加`

**悪いコミットの例:**
- 複数の無関係な変更をまとめたコミット
- テスト・ビルドが失敗したままのコミット
- 説明が不十分なコミットメッセージ

### 開発作者情報
- **開発者**: ema
- **GitHub**: [emar27181](https://github.com/emar27181)
- **連絡先**: GitHubプロフィール参照### ✅ 解決済みの問題

#### 問題: 画面が真っ白になり地図が表示されない (2026/02/01解決)

**現象:**
- ローカル開発環境でページを開いても、何も表示されず画面が真っ白になる。
- ブラウザのコンソールに `Uncaught ReferenceError: tokyoStation is not defined` というエラーが表示される。

**原因:**
- `src/components/RailwayMap.tsx` の `MapContainer` コンポーネントにて、地図の中心座標を渡す `center` プロパティに、未定義の変数 `tokyoStation` が指定されていた。

**解決策:**
- `RailwayMap.tsx` コンポーネント内にすでに定義されていた、地図の中心座標を管理するstate変数 `mapCenter` を `center` プロパティに設定するように修正。
- これにより、アプリケーションは正しく初期化され、地図が表示されるようになった。
